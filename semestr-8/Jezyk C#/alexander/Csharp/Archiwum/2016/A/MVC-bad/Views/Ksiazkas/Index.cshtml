@model IEnumerable<MVC.Models.Ksiazka>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tytul)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.autor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.autor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ksiazkaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ksiazkaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ksiazkaId })
        </td>
    </tr>
}

</table>
